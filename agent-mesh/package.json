{
  "name": "aura-agentic",
  "version": "1.0.0",
  "description": "AuraAgentic — Autonomous multi-agent task economy on Somnia Agentic L1",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.js --network somnia_testnet",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "node": "hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "ethers": "^6.13.0",
    "dotenv": "^16.4.0",
    "axios": "^1.7.0"
  }
}
